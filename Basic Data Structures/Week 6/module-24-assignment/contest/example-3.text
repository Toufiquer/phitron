   // !
   int n;
   cin >> n;
   int a[n];
   for (int i = 0; i < n;i++){
       cin >> a[i];
   }
   Node *root = convert(a, n, 0, n-1);
//    for (int i = 0; i < n;i++){
//        cout << a[i] << " ";
//    }
   printTree(root);
   cout << endl<< " v 1.0.0.1";